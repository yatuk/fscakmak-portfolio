---
import BaseLayout from '@layouts/BaseLayout.astro';
import TerminalShell from '@components/TerminalShell.astro';
import { getProfile } from '@lib/profile';

const locale = 'tr';
const title = 'Fatih Serdar Çakmak — SOC Analisti & AI/LLM Güvenliği | İTÜ Bilgisayar Müh.';
const description =
  "Fibabanka'da SOC Analist Stajyeri, İTÜ Bilgisayar Mühendisliği öğrencisi; açık kaynak AI/LLM güvenlik araçları geliştiriyor (Tamga, MCPRadar). SIEM, SOAR, detection, MCP güvenliği. İstanbul.";

const profile = getProfile(locale);
---

<BaseLayout title={title} description={description} locale={locale} hideHeaderControls={true}>
  <!-- Crawlable summary — the terminal renders everything with JS,
       which most AI crawlers never execute. Screen readers get a
       usable overview from the same block. -->
  <section class="sr-only">
    <h1>{profile.identity.name} — {profile.identity.role}</h1>
    <p>{profile.about}</p>
    <h2>Projeler</h2>
    <ul>
      {profile.projects.map((p) => (
        <li>
          {p.url ? <a href={p.url}>{p.name}</a> : p.name}: {p.summary}
        </li>
      ))}
    </ul>
    <p>
      <a href="/tr/cv">CV</a> · <a href="/tr/writeups">Yazılar</a> ·{' '}
      <a href={`https://github.com/${profile.identity.social.github}`}>GitHub</a> ·{' '}
      <a href={`https://linkedin.com/in/${profile.identity.social.linkedin}`}>LinkedIn</a>
    </p>
  </section>

  <TerminalShell locale={locale} />
</BaseLayout>
