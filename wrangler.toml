# Cloudflare Workers static assets + Pages output (Astro SSG).
# Git CI runs: npm run build → npm run deploy
name = "fscakmak-portfolio"
compatibility_date = "2025-01-15"
pages_build_output_dir = "./dist"

[assets]
directory = "./dist"
